# This Program (add-on) for pyinstaller a python libary that lets you build apps (exe or dmg) the normal pyinstaller makes dist folders and a bunch of junk. What this Program does is when it builds it forces pyinstaller to put its files into %temp% and the exe goes on to the User's Desktop and then it delete the temp files it made.
$ Download it Here:
